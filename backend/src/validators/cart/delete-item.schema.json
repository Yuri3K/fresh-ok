{
  "$id": "validators/reviews/delete-item.schema.json",
  "title": "Delete item from Cart",
  "type": "object",
  "required": ["productId"],
  "additionalProperties": false,
  "properties": {
    "productId": {
      "type": "string",
      "minLength": 1,
      "description": "Firebase ID of the product for deleting item from cart"
    }
  }
}